# AI Notes

This documentation set is trimmed to the active plan only.

Focus areas:
- WordPress + ROLLER + PATCH integration
- Glasgow pilot execution
- UK-first rollout
- Current TODO, technical stack, and implementation plan in guides.
- Keep the TODO document in guides up to date; treat it as the current source of implementation status.
- Keep business constants in code or provider configuration unless a dedicated source-of-truth document is explicitly added.
- In narrative docs and diagrams, prefer neutral terms like `threshold` unless the value is enforced by code or schema.

When updating docs:
- Keep content current-state, not historical.
- Keep wording implementation-oriented.
- Default to plain, meeting-ready wording:
  - one question per line
  - one idea per question
  - minimal jargon
  - technical detail only when needed
- Keep source links in one source list document.
